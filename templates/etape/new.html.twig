{% extends 'base.html.twig' %}
{% block title %}New Etape{% endblock %}

{% block body %}
    <h1>{{ etape.getVoyage().getLibelle() }}</h1>
    <hr>
    <h2>Create new Etape</h2>

    {{ include('etape/_form.html.twig') }}

    <a class="btn btn-danger" href="{{ path('app_voyages_index') }}">back to list</a>
{% endblock %}
